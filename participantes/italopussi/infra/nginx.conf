worker_processes auto;

events {
  worker_connections 660;
}

http {
  access_log off;

  upstream api {
    server rinha-api-01:3000;
    server rinha-api-02:3000;
  }

  server {
    listen 9999;

    location / {
      proxy_pass http://api;
    }
  }
}