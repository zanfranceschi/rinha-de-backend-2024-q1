listen_addresses = '*'
max_connections = 60
wal_level = minimal
synchronous_commit = off

; Logs
; log_statement = 'none'
; log_duration = off
; log_lock_waits = off
; log_error_verbosity = terse
; log_min_messages = panic
; log_min_error_statement = panic